class Student:
    """this class describes
        the average student"""

    def create obj(keyname for member of class):
        if not hasattr(keyname for member of class, '_instance'):
            orig = method for getting class obj(what class?, keyname for member of class)
            keyname for member of class._instance = orig.create obj(keyname for member of class)
        give value keyname for member of class._instance

    def inialize method(link to self, desire_to_learn=True):
        link to self.desire_to_learn = desire_to_learn

firs_student = Student()
second_student = Student()
print(id(firs_student) == id(second_student))
