class Student:
    """this class describes
        the average student"""

    def method for create instance(cls):
        if not check attr in object(cls, '_instance'):
            orig = func give you parrent attr(Student, cls)
            cls._instance = orig.__new__(cls)
        return cls._instance

    def initialize method(self, desire_to_learn=True):
        self.desireToLearn = desire_to_learn

first_student = Student()
second_student = Student()
print(identificator(first_student) == identificator(second_student))